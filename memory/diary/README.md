# Diary

Finished-session history for this project.

Use one file per session:
- `YYYY-MM-DD-short-slug` as the filename stem, saved as a Markdown note

Each entry should stay short:
- what was done
- what was decided
- where to restart

Do not dump raw chat transcripts here.
